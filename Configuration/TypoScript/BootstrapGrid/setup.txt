
/**
 * plugin.tx_adxless
 */
plugin.tx_adxless {

	lessphp {

		variables {

			classPrefix = {$plugin.tx_adxless.bootstrapGrid.cssClassPrefix}

			gridColumns = {$plugin.tx_adxless.bootstrapGrid.columns}

			// default (940px grid)
			gridDefaultColumnWidth = {$plugin.tx_adxless.bootstrapGrid.default.columnWidth}
			gridDefaultGutterWidth = {$plugin.tx_adxless.bootstrapGrid.default.gutter}
			gridDefaultRowWidth = (@gridColumns * @gridDefaultColumnWidth) + (@gridDefaultGutterWidth * (@gridColumns - 1))

			// large (1200px min)
			gridLargeColumnWidth = {$plugin.tx_adxless.bootstrapGrid.large.columnWidth}
			gridLargeGutterWidth = {$plugin.tx_adxless.bootstrapGrid.large.gutter}
			gridLargeBreakpointMin = {$plugin.tx_adxless.bootstrapGrid.large.breakpointMin}
			gridLargeRowWidth = (@gridColumns * @gridLargeColumnWidth) + (@gridLargeGutterWidth * (@gridColumns - 1))

			// medium (768px-979px)
			gridMediumColumnWidth = {$plugin.tx_adxless.bootstrapGrid.medium.columnWidth}
			gridMediumGutterWidth = {$plugin.tx_adxless.bootstrapGrid.medium.gutter}
			gridMediumBreakpointMin = {$plugin.tx_adxless.bootstrapGrid.medium.breakpointMin}
			gridMediumBreakpointMax = {$plugin.tx_adxless.bootstrapGrid.medium.breakpointMax}
			gridMediumRowWidth = (@gridColumns * @gridMediumColumnWidth) + (@gridMediumGutterWidth * (@gridColumns - 1))

			// fluid default
			fluidGridDefaultColumnWidth = percentage(@gridDefaultColumnWidth / @gridDefaultRowWidth)
			fluidGridDefaultGutterWidth = percentage(@gridDefaultGutterWidth / @gridDefaultRowWidth)

			// fluid large (1200px min)
			fluidGridLargeColumnWidth = percentage(@gridLargeColumnWidth / @gridLargeRowWidth)
			fluidGridLargeGutterWidth = percentage(@gridLargeGutterWidth / @gridLargeRowWidth)

			// fluid medium (768px-979px)
			fluidGridMediumColumnWidth = percentage(@gridMediumColumnWidth / @gridMediumRowWidth)
			fluidGridMediumGutterWidth = percentage(@gridMediumGutterWidth / @gridMediumRowWidth)
		}
	}
}


// Page: includes
page {

	includeCSS {
		tx_adxless_bootstrap_grid_common = EXT:adx_less/Resources/Private/LESS/BootstrapGrid/Common.less
	}

	headerData.1352997637 = TEXT
	headerData.1352997637 {

		value = {$plugin.tx_adxless.bootstrapGrid.viewport}
		required = 1

		wrap = <meta name="viewport" content=" | ">

		prepend = TEXT
		prepend.char = 10
	}
}